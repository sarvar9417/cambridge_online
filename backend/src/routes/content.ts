import{Router}from'express';import{z}from'zod';import{ContentService}from'../services/content-service.js';export function createContentRouter(s:ContentService){const r=Router();r.get('/',async(q,p)=>p.json({data:await s.list(q.actor!)}));r.get('/games',async(q,p)=>p.json({data:await s.games(q.actor!)}));r.get('/flashcards/due',async(q,p)=>p.json({data:await s.due(q.actor!)}));r.post('/flashcards/:id/review',async(q,p)=>{const b=z.object({grade:z.number().int().min(0).max(5)}).parse(q.body);p.json(await s.review(q.actor!,z.string().uuid().parse(q.params.id),b.grade))});return r}
