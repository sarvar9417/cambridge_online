import{Router}from'express';import{z}from'zod';import{IngestionService}from'../services/ingestion-service.js';
const uuid=z.string().uuid(),findingCode=z.string().regex(/^[A-Z]\d{2}$/),commandWord=z.enum(['State','Give','Name','Identify','Define','Describe','Explain','Compare','Calculate','Complete','Draw','Write','Evaluate','Justify','Suggest','Show','Other']);export function createIngestionRouter(s:IngestionService){const r=Router();r.post('/papers',async(q,p)=>{const b=z.object({syllabusId:uuid,componentId:uuid,year:z.number().int().min(2000).max(2100),series:z.enum(['MJ','ON','FM']),variant:z.number().int().min(1).max(9),kind:z.enum(['QP','MS','IN','ER','GT']),storagePath:z.string().min(1).max(500),sha256:z.string().regex(/^[a-f0-9]{64}$/)}).parse(q.body);p.status(202).json(await s.register(q.actor!,b))});r.get('/jobs',async(q,p)=>p.json({data:await s.jobs(q.actor!)}));r.get('/review',async(q,p)=>{const query=z.object({findingCode:findingCode.optional(),limit:z.coerce.number().int().min(1).max(100).optional()}).parse(q.query);p.json({data:await s.review(q.actor!,query)})});r.post('/review/bulk-approve',async(q,p)=>{const b=z.object({findingCode}).parse(q.body);p.json(await s.bulkApprove(q.actor!,b.findingCode))});r.post('/review/undo',async(q,p)=>p.json(await s.undo(q.actor!)));r.patch('/review/:id',async(q,p)=>{const b=z.object({stemMd:z.string().trim().min(10).max(3000),marks:z.number().int().min(0).max(100).nullable(),commandWord:commandWord.nullable()}).parse(q.body);p.json(await s.edit(q.actor!,uuid.parse(q.params.id),{stemMd:b.stemMd,marks:b.marks??null,commandWord:b.commandWord??null}))});r.post('/review/:id/:decision',async(q,p)=>p.json(await s.decide(q.actor!,uuid.parse(q.params.id),z.enum(['approved','rejected']).parse(q.params.decision))));return r}
