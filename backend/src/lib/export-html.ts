const esc=(s:unknown)=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
export interface ExportQuestion{displayRef:string;stem:string;context?:string;marks:number;points?:Array<{code:string;text:string;marks:number}>}
export function renderPaperHtml(title:string,questions:ExportQuestion[],includeScheme=false){const total=questions.reduce((n,q)=>n+q.marks,0);return`<!doctype html><html><head><meta charset="utf-8"><style>@page{size:A4;margin:18mm}body{font-family:Arial,sans-serif;color:#111;font-size:11pt}h1{font-size:18pt;border-bottom:2px solid #111;padding-bottom:8px}.meta{display:flex;justify-content:space-between}.question{break-inside:avoid;margin:18px 0}.ref{font-weight:bold}.marks{float:right}.context{white-space:pre-wrap}.scheme{margin:8px 0 0 24px}.scheme li{margin:5px 0}footer{position:fixed;bottom:0;font-size:9pt}</style></head><body><h1>${esc(title)}</h1><div class="meta"><span>Cambridge International AS & A Level Computer Science</span><strong>Total: ${total}</strong></div>${questions.map(q=>`<section class="question"><div><span class="ref">${esc(q.displayRef)}</span><span class="marks">[${q.marks}]</span></div>${q.context?`<p class="context">${esc(q.context)}</p>`:''}<p>${esc(q.stem)}</p>${includeScheme?`<ol class="scheme">${(q.points??[]).map(p=>`<li>${esc(p.code)} ${esc(p.text)} [${p.marks}]</li>`).join('')}</ol>`:''}</section>`).join('')}<footer>Generated by CamPath</footer></body></html>`}
