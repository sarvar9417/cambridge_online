import{describe,expect,it}from'vitest';import{validateContent,type ContentInput}from'./content-validation.js';const base=():ContentInput=>({notes:Array(610).fill('database').join(' '),learningObjectiveKeywords:[['database']],glossary:[{term:'database',definitionEn:'organized collection of data'}],officialText:'organized collection of data',flashcards:Array.from({length:8},(_,i)=>({front:`question ${i}`})),quiz:[{options:['a','b','c','d'],correctIds:[0]}],englishTerms:['database'],allowedTerms:['database']});describe('C01-C10',()=>{const cases:Array<[string,(x:ContentInput)=>void]>=[['C01',x=>x.notes='short'],['C02',x=>x.learningObjectiveKeywords=[['network']]],['C03',x=>x.glossary[0]!.term='network'],['C04',x=>x.glossary[0]!.definitionEn='totally unrelated phrase'],['C05',x=>x.flashcards=[]],['C06',x=>x.flashcards[1]!.front=x.flashcards[0]!.front],['C07',x=>x.quiz[0]!.correctIds=[]],['C08',x=>x.quiz[0]!.options=['a','a']],['C09',x=>x.englishTerms=['network']],['C10',x=>{x.englishTerms=['database'];x.allowedTerms=[]}]];it.each(cases)('%s triggers',(code,mutate)=>{const x=base();mutate(x);expect(validateContent(x).map(f=>f.code)).toContain(code)});it('clean content passes',()=>expect(validateContent(base())).toEqual([]))});
