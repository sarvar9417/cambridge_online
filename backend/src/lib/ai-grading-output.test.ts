import{describe,expect,it}from'vitest';import{validateAiOutput}from'./ai-grading-output.js';import type{Scheme}from'./marking.js';const scheme:Scheme={type:'all_required',maxMarks:2,points:[{code:'MP1',marks:1,groupId:null,requires:[]},{code:'MP2',marks:1,groupId:null,requires:[]}],groups:[],levels:[]};describe('AI grading output validation',()=>{it('accepts evidence present after normalization',()=>expect(validateAiOutput('A UNIQUE identifier.',scheme,{points:[{code:'MP1',matched:true,evidence:'unique identifier',confidence:.9},{code:'MP2',matched:false,confidence:.8}],feedback_uz:'Yaxshi'}).matched[0]!.matched).toBe(true));it('rejects fabricated evidence',()=>{const r=validateAiOutput('other words',scheme,{points:[{code:'MP1',matched:true,evidence:'unique identifier'}]});expect(r.matched[0]!.matched).toBe(false);expect(r.needsTeacher).toBe(true)});it('fills missing points as false',()=>expect(validateAiOutput('',scheme,{points:[]}).matched).toHaveLength(2));it('ignores unknown points with finding',()=>expect(validateAiOutput('',scheme,{points:[{code:'MP9',matched:false}]}).findings).toContain('unknown_mark_point:MP9'));it('clamps confidence',()=>expect(validateAiOutput('',scheme,{points:[{code:'MP1',matched:false,confidence:2},{code:'MP2',matched:false,confidence:-1}]}).matched.map(x=>x.confidence)).toEqual([1,0]));it('requires evidence for true point',()=>expect(validateAiOutput('',scheme,{points:[{code:'MP1',matched:true},{code:'MP2',matched:false}]}).findings).toContain('missing_evidence:MP1'));it('provides fallback feedback',()=>expect(validateAiOutput('',scheme,{points:[]}).feedback.length).toBeGreaterThan(0));it('caps feedback at 800 characters',()=>expect(validateAiOutput('',scheme,{points:[],feedback_uz:'a'.repeat(900)}).feedback).toHaveLength(800));it('levels always needs teacher',()=>expect(validateAiOutput('',{...scheme,type:'levels_of_response'},{points:[]}).needsTeacher).toBe(true))});
