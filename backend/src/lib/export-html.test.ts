import{describe,expect,it}from'vitest';import{renderPaperHtml}from'./export-html.js';describe('paper HTML',()=>{it('shows total marks',()=>expect(renderPaperHtml('Test',[{displayRef:'Q1',stem:'A',marks:2},{displayRef:'Q2',stem:'B',marks:3}])).toContain('Total: 5'));it('keeps question in a non-breaking section',()=>expect(renderPaperHtml('T',[{displayRef:'Q1',stem:'A',marks:1}])).toContain('break-inside:avoid'));it('escapes untrusted text',()=>expect(renderPaperHtml('<script>',[{displayRef:'Q',stem:'<img>',marks:1}])).not.toContain('<script>'));it('includes scheme only when requested',()=>{const q=[{displayRef:'Q',stem:'A',marks:1,points:[{code:'MP1',text:'Point',marks:1}]}];expect(renderPaperHtml('T',q,true)).toContain('MP1');expect(renderPaperHtml('T',q,false)).not.toContain('MP1')})});
