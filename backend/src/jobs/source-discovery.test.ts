import{mkdtemp,mkdir,rm,writeFile}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import{afterEach,describe,expect,it}from'vitest';import{discoverCorpusSources,parseCambridgeSourceFilename}from'./source-discovery.js';
const dirs:string[]=[];afterEach(async()=>Promise.all(dirs.splice(0).map(path=>rm(path,{recursive:true,force:true}))));async function tree(){const root=await mkdtemp(join(tmpdir(),'campath-discover-'));dirs.push(root);const a=join(root,'2025_May_June'),b=join(root,'nested','2024_ON');await mkdir(a,{recursive:true});await mkdir(b,{recursive:true});for(const path of[join(a,'9618_s25_qp_11.pdf'),join(a,'9618_s25_ms_11.pdf'),join(a,'9618_s25_qp_12.pdf'),join(a,'9618_s25_gt.pdf'),join(a,'9618_s25_in_21.pdf'),join(b,'9618_w24_qp_43.pdf'),join(b,'9618_w24_ms_43.pdf')])await writeFile(path,'pdf');return root}
describe('Cambridge source discovery',()=>{
 it('parses canonical 9618 season/year/kind/paper codes',()=>{expect(parseCambridgeSourceFilename('9618_s25_qp_11.pdf','/x/a.pdf')).toEqual({syllabusCode:'9618',year:2025,series:'MJ',paperCode:11,kind:'QP',path:'/x/a.pdf'});expect(parseCambridgeSourceFilename('9618_W24_MS_43.PDF','/x/b.pdf')).toEqual({syllabusCode:'9618',year:2024,series:'ON',paperCode:43,kind:'MS',path:'/x/b.pdf'});expect(parseCambridgeSourceFilename('9618_m23_qp_21.pdf','/x/c.pdf')).toEqual({syllabusCode:'9618',year:2023,series:'FM',paperCode:21,kind:'QP',path:'/x/c.pdf'});expect(parseCambridgeSourceFilename('9618_s25_gt.pdf','/x/gt.pdf')).toBeNull()});
 it('recursively discovers only QP/MS PDFs and reports unpaired variants',async()=>{const root=await tree(),result=await discoverCorpusSources(root);expect(result.items.map(item=>`${item.year}-${item.series}-${item.paperCode}-${item.kind}`)).toEqual(['2024-ON-43-MS','2024-ON-43-QP','2025-MJ-11-MS','2025-MJ-11-QP','2025-MJ-12-QP']);expect(result.pairCount).toBe(2);expect(result.unpaired).toEqual([{key:'2025-MJ-12',missing:'MS'}]);expect(result.ignoredPdfCount).toBe(2)});
 it('fails when the same canonical QP or MS appears twice in nested folders',async()=>{const root=await mkdtemp(join(tmpdir(),'campath-discover-'));dirs.push(root);const a=join(root,'a'),b=join(root,'b');await mkdir(a);await mkdir(b);await writeFile(join(a,'9618_s25_qp_11.pdf'),'x');await writeFile(join(b,'9618_s25_qp_11.pdf'),'x');await expect(discoverCorpusSources(root)).rejects.toThrow('duplicate_discovered_source:2025-MJ-11-QP')});
});
