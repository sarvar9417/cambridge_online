import type{Pool}from'pg';import{imageBlock,loadPrompt,recordAiCall,recordAiFailure,type AiUsage,type ClaudeUserBlock}from'../../lib/ai/claude.js';import type{IngestionStageHandler}from'./ingestion.js';import{mergeQuestions,normalizeQp,type ExtractedQuestion}from'./ingestion-contract.js';
type Artifact=Record<string,unknown>;type Batch={pageNumbers:number[];images:string[];text:string};type Prepared={paperId:string;batches:Batch[];[key:string]:unknown};type AiClient={model:string;complete<T>(input:{purpose:string;prompt:{version:string;body:string};content:ClaudeUserBlock[];maxTokens?:number}):Promise<{data:T;usage:AiUsage;raw:unknown}>};type ImageLoader=(path:string)=>Promise<ClaudeUserBlock>;
export function createQuestionExtractionV2Handler(pool:Pool,client:AiClient,imageLoader:ImageLoader=imageBlock):IngestionStageHandler{return(_refId,input)=>extractQuestionsV2Stage(pool,client,input,imageLoader)}
export async function extractQuestionsV2Stage(pool:Pool,client:AiClient,input:Artifact,imageLoader:ImageLoader=imageBlock):Promise<Artifact>{const prepared=pickPrepared(input);if(!prepared)return input;const metadata=await paperMetadata(pool,prepared.paperId);if(metadata.kind!=='QP')return input;const prompt=await loadPrompt('extract-question',4);let questions:ExtractedQuestion[]=[],carryoverPaths=new Set<string>();const conflicts:string[]=[],batchTotals:number[]=[];for(const batch of prepared.batches){const priorRefs=questions.filter(question=>!carryoverPaths.has(question.path)).map(question=>question.path),content=await extractionContent(batch,{metadata,prior_refs:priorRefs,carryover_refs:[...carryoverPaths]},imageLoader),started=Date.now();let response:{data:unknown;usage:AiUsage;raw:unknown};try{response=await client.complete<unknown>({purpose:'extract_qp',prompt,content,maxTokens:16384});await recordAiCall(pool,response.usage,{table:'source_papers',id:prepared.paperId})}catch(error){await recordAiFailure(pool,{purpose:'extract_qp',model:client.model,promptVersion:prompt.version,ref:{table:'source_papers',id:prepared.paperId},error,latencyMs:Date.now()-started});throw error}const normalized=normalizeQp(response.data),incomingPaths=new Set(normalized.questions.map(question=>question.path)),mergeBase=questions.filter(question=>!(carryoverPaths.has(question.path)&&incomingPaths.has(question.path))),merged=mergeQuestions(mergeBase,normalized.questions);questions=merged.questions;conflicts.push(...merged.conflicts);batchTotals.push(normalized.pageTotalMarks);const nextCarryover=new Set<string>();for(const path of carryoverPaths)if(!incomingPaths.has(path))nextCarryover.add(path);if(normalized.truncated){const boundaryPage=Math.max(...batch.pageNumbers);for(const question of normalized.questions)if(question.sourcePages.includes(boundaryPage))nextCarryover.add(question.path)}carryoverPaths=nextCarryover}if(carryoverPaths.size)questions=questions.map(question=>carryoverPaths.has(question.path)?{...question,confidence:Math.min(question.confidence,.79),issues:[...new Set([...question.issues,'unresolved_truncated_carryover'])]}:question);return{...input,questions,qp:{...prepared,extraction:{promptVersion:prompt.version,questionCount:questions.length,batchTotals,overlapConflicts:[...new Set(conflicts)],unresolvedCarryoverPaths:[...carryoverPaths]}}}}
function pickPrepared(input:Artifact):Prepared|null{const nested=input.qp;return isPrepared(nested)?nested:isPrepared(input)?input:null}function isPrepared(value:unknown):value is Prepared{return Boolean(value&&typeof value==='object'&&typeof(value as Prepared).paperId==='string'&&Array.isArray((value as Prepared).batches))}
async function paperMetadata(pool:Pool,paperId:string){const result=await pool.query(`select sp.id,sp.kind::text kind,sp.year,sp.series::text series,sp.variant,s.code syllabus,c.number component,c.name component_name,c.level::text level,c.total_marks from source_papers sp join syllabi s on s.id=sp.syllabus_id join components c on c.id=sp.component_id where sp.id=$1`,[paperId]);if(!result.rowCount)throw new Error('ingestion_paper_not_found');return result.rows[0]as{kind:string;year:number;series:string;variant:number;syllabus:string;component:number;component_name:string;level:string;total_marks:number}}
async function extractionContent(batch:Batch,header:Record<string,unknown>,imageLoader:ImageLoader):Promise<ClaudeUserBlock[]>{const textPages=batch.text.split('\n\f\n'),content:ClaudeUserBlock[]=[{type:'text',text:`Input metadata:\n${JSON.stringify(header)}`}];for(let index=0;index<batch.images.length;index++){content.push(await imageLoader(batch.images[index]!));content.push({type:'text',text:`## Text layer, page ${batch.pageNumbers[index]??index+1}\n\n${textPages[index]??''}`})}return content}
