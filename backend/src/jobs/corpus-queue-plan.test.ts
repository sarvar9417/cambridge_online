import{describe,expect,it}from'vitest';import{buildCorpusQueuePlan}from'./corpus-queue-plan.js';
const row=(key:string,status:'COMPLETE'|'READY_TO_QUEUE'|'SOURCE_MISSING',msPaperId:string|null='ms')=>({key,year:2025,series:'MJ'as const,component:1,variant:1,paperCode:11,qpPaperId:`qp-${key}`,msPaperId,qpPath:'qp.pdf',msPath:msPaperId?'ms.pdf':null,qpExists:status!=='SOURCE_MISSING',msExists:status!=='SOURCE_MISSING',qpPages:8,msPages:6,rootCount:8,leafCount:24,leafMarks:75,expectedMarks:75,markSchemeCount:24,approvedLeafCount:24,unresolvedErrors:0,status,minimumExtractionCalls:7,knownClassificationCalls:24,knownDependencyCalls:8,knownCrosscheckCalls:24});
describe('corpus queue plan',()=>{
 it('queues only ready canonical QP/MS pairs',()=>{const plan=buildCorpusQueuePlan([row('ready','READY_TO_QUEUE'),row('complete','COMPLETE'),row('missing','SOURCE_MISSING',null)]);expect(plan.items).toEqual([{key:'ready',qpPaperId:'qp-ready',msPaperId:'ms',reason:'incomplete_or_unreviewed'}]);expect(plan.skippedComplete).toEqual(['complete']);expect(plan.blockedSources).toEqual(['missing'])});
 it('never queues a row without an MS id even if status was mislabelled ready',()=>{const plan=buildCorpusQueuePlan([row('bad','READY_TO_QUEUE',null)]);expect(plan.items).toEqual([]);expect(plan.blockedSources).toEqual(['bad'])});
});
