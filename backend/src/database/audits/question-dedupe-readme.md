# Question duplicate audit scope

A row is a destructive duplicate candidate only when it is redundant inside the same official source paper after comparing the complete question body, structured content, assets, mark scheme, taxonomy and learning-objective mappings. Identical content in different official Cambridge variants is a source-equivalent occurrence and must not be deleted because each source paper/ref remains part of the canonical corpus.
