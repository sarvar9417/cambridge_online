import type{Pool}from'pg';import type{Actor}from'../lib/actor.js';import{sm2}from'../lib/srs.js';import{DomainError}from'./assignments-service.js';
export class ContentService{constructor(private pool:Pool){}private student(a:Actor){if(a.role!=='student')throw new DomainError('students_only',403)}async due(a:Actor){this.student(a);const r=await this.pool.query(`select fr.flashcard_id,fc.front_md,fc.back_md,fc.hint_md,fr.ease_factor,fr.interval_days,fr.repetitions,fr.lapses,fr.due_at from flashcard_reviews fr join flashcards fc on fc.id=fr.flashcard_id where fr.user_id=$1 and fr.due_at<=now() order by fr.due_at limit 50`,[a.id]);return r.rows}async review(a:Actor,cardId:string,grade:number){this.student(a);const r=await this.pool.query(`select ease_factor,interval_days,repetitions,lapses from flashcard_reviews where user_id=$1 and flashcard_id=$2`,[a.id,cardId]);if(!r.rowCount)throw new DomainError('not_found',404);const next=sm2({easeFactor:Number(r.rows[0].ease_factor),intervalDays:r.rows[0].interval_days,repetitions:r.rows[0].repetitions,lapses:r.rows[0].lapses},grade);await this.pool.query(`update flashcard_reviews set ease_factor=$3,interval_days=$4,repetitions=$5,lapses=$6,due_at=$7,last_grade=$8,last_reviewed_at=now()where user_id=$1 and flashcard_id=$2`,[a.id,cardId,next.easeFactor,next.intervalDays,next.repetitions,next.lapses,next.dueAt,next.lastGrade]);return next}async games(a:Actor){this.student(a);const glossary=await this.pool.query(`select distinct gt.id,gt.term,gt.definition_en definition from glossary_terms gt join subtopics st on st.id=gt.subtopic_id join topics t on t.id=st.topic_id join classes c on c.syllabus_id=t.syllabus_id join enrollments e on e.class_id=c.id and e.left_at is null where e.student_id=$1 and gt.status='approved' order by gt.term limit 8`,[a.id]);const objectives=await this.pool.query(`select distinct lo.id,lo.code,lo.text,lo.sort_order from learning_objectives lo join subtopics st on st.id=lo.subtopic_id join topics t on t.id=st.topic_id join classes c on c.syllabus_id=t.syllabus_id join enrollments e on e.class_id=c.id and e.left_at is null where e.student_id=$1 order by lo.sort_order,lo.code limit 6`,[a.id]);const terms=glossary.rows.map(row=>({id:row.id,term:row.term,definition:row.definition}));return{termMatch:terms,sequence:objectives.rows.map(row=>({id:row.id,code:row.code,text:row.text})),spotTheGap:terms.map(row=>({id:row.id,prompt:`_____ — ${row.definition}`,answer:row.term}))}}async list(a:Actor){const r=await this.pool.query(`select ci.id,ci.kind,ci.title,ci.body_md,st.code subtopic_code,st.title subtopic_title from content_items ci join subtopics st on st.id=ci.subtopic_id where ci.status='approved' order by st.code,ci.sort_order`);return r.rows}}
