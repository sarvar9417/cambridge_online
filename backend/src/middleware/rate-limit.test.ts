import express from'express';import request from'supertest';import{beforeEach,describe,expect,it,vi}from'vitest';import{clearRateLimits,rateLimit}from'./rate-limit.js';import{durableRateLimit}from'./durable-rate-limit.js';describe('rate limiter',()=>{beforeEach(clearRateLimits);it('allows requests under limit',async()=>{const app=express();app.use(rateLimit({windowMs:1000,max:2}));app.get('/',(_q,r)=>r.sendStatus(204));expect((await request(app).get('/')).status).toBe(204);expect((await request(app).get('/')).status).toBe(204)});it('returns 429 over limit',async()=>{const app=express();app.use(rateLimit({windowMs:1000,max:1}));app.get('/',(_q,r)=>r.sendStatus(204));await request(app).get('/');const r=await request(app).get('/');expect(r.status).toBe(429);expect(r.headers['retry-after']).toBeDefined()});it('uses the database bucket atomically and returns 429 after the durable limit',async()=>{const query=vi.fn().mockResolvedValue({rows:[{request_count:3,retry_after:42}]});const app=express();app.use(durableRateLimit({query}as never,{windowMs:60000,max:2,key:(req)=>`${req.actor?.id}:${req.ip}`}));app.get('/',(_q,r)=>r.sendStatus(204));const response=await request(app).get('/');expect(response.status).toBe(429);expect(response.headers['retry-after']).toBe('42');expect(query).toHaveBeenCalledWith(expect.stringContaining('on conflict(bucket_key) do update'),[expect.stringContaining('undefined:::ffff:127.0.0.1'),60000])})});
