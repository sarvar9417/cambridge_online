import{describe,expect,it,vi}from'vitest';import{opportunisticMaintenance}from'./opportunistic-maintenance.js';describe('opportunistic maintenance',()=>{it('deduplicates maintenance inside the interval',async()=>{const close=vi.fn().mockResolvedValue(0),next=vi.fn(),middleware=opportunisticMaintenance(close,60_000);await middleware({}as any,{}as any,next);await middleware({}as any,{}as any,next);expect(close).toHaveBeenCalledTimes(1);expect(next).toHaveBeenCalledTimes(2)});it('does not block requests when maintenance fails',async()=>{const log=vi.spyOn(console,'error').mockImplementation(()=>{}),next=vi.fn(),middleware=opportunisticMaintenance(vi.fn().mockRejectedValue(Error('db')));await middleware({}as any,{}as any,next);expect(next).toHaveBeenCalled();expect(log).toHaveBeenCalled();log.mockRestore()})});
