import type{IncomingMessage,ServerResponse}from'node:http';import{pool}from'../backend/src/database/client.js';import{AssignmentsService}from'../backend/src/services/assignments-service.js';
export default async function handler(req:IncomingMessage,res:ServerResponse){if(!process.env.CRON_SECRET||req.headers.authorization!==`Bearer ${process.env.CRON_SECRET}`){res.statusCode=401;res.end('Unauthorized');return}if(!pool){res.statusCode=503;res.end('Database unavailable');return}const closed=await new AssignmentsService(pool).closeExpired(500);res.setHeader('Content-Type','application/json');res.end(JSON.stringify({closed}))}
