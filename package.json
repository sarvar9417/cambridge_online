{
  "name": "campath",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "test:inventory": "python3 -m unittest discover -s scripts -p 'test_*.py'",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend && tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --esModuleInterop --skipLibCheck 'api/[...path].ts' api/maintenance.ts",
    "vercel-build": "npm run build -w frontend",
    "verify": "npm run typecheck && npm test && npm run test:inventory && npm run build"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "concurrently": "^9.2.1"
  }
}
